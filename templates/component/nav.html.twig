{% import "macros/link.macro.twig" as link %}


<nav class="navbar navbar-expand-lg sk-nav">
    <div class="container-fluid">
        <a class="navbar-brand text-white" href="{{ path('app_landing_page') }}">Odyssey</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasNavbar" aria-controls="offcanvasNavbar" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="offcanvas offcanvas-end w-75" tabindex="-1" id="offcanvasNavbar" aria-labelledby="offcanvasNavbarLabel">
            <div class="offcanvas-header sk-nav">
                <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
            </div>
            <div class="offcanvas-body sk-nav">
                <ul class="navbar-nav justify-content-end flex-grow-1 pe-3">
                    <li class="nav-item">
                        <a class="nav-link text-white" href="{{ path('app_landing_page') }}">Home</a>
                    </li>
                    {% if (app.user) %}
                    <li class="nav-item">
                        <a class="nav-link text-white" href="{{ path('app_user') }}">Mon profil</a>
                    </li>
                    {% endif %}
                    <li class="nav-item">
                        <a class="nav-link text-white" href="{{ path('app_landing_page') }}#contactForm">Contact</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</nav>
